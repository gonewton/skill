[metadata]
id = "newton"
version = "1.1.0"
name = "newton"
description = "Newton CLI for iterative optimization and workflow automation. Use for classic run/step/batch loops and workflow-graph operations (run, lint, explain, resume, checkpoints, artifacts, webhook)."
author = "gonewton"
tags = ["tools", "newton", "optimization", "cli", "evaluator-advisor-executor", "workspace"]
capabilities = ["run-step-batch-status-report-error", "workspace-config", "tools-config", "cli-commands"]

[dependencies]

[tool.fastskill]
skills_directory = "newton"
