# `newton step` (removed)

The **`step`** subcommand is not part of the current Newton CLI.

Run one logical slice of work with **`newton run <workflow.yaml>`** (workflows can model single-step or multi-step graphs), or use **`newton batch`** for queued plans. See [run.md](run.md) and [batch.md](batch.md).
