# `newton report` (removed)

The **`report`** subcommand is not part of the current Newton CLI.

Summaries come from **workflow outputs**, **artifacts**, **checkpoint or execution records**, and external tooling (logs, CI, or `newton serve` consumers). See [run.md](run.md) and repository documentation for workflow observability.
